#
# Treat this board as a standalone (no split) keyboard
#
CONFIG_ZMK_SPLIT=n             # disable split-keyboard support entirely :contentReference[oaicite:1]{index=1}

#
# Disable all Bluetooth support
#
CONFIG_BT=n                    # turn off Zephyr’s Bluetooth controller :contentReference[oaicite:2]{index=2}
CONFIG_ZMK_BLE=n               # turn off ZMK’s BLE HID profile :contentReference[oaicite:3]{index=3}

#
# Enable USB-only HID keyboard
#
CONFIG_USB=y                   # enable Zephyr’s USB device stack :contentReference[oaicite:4]{index=4}
CONFIG_ZMK_USB=y               # enable ZMK’s USB HID keyboard functionality :contentReference[oaicite:5]{index=5}

